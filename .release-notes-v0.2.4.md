# Release v0.2.4

This release updates the version to 0.2.4 across all configuration files.

## Changes
- Updated package.json version to 0.2.4
- Updated src-tauri/tauri.conf.json version to 0.2.4
- Updated src-tauri/Cargo.toml version to 0.2.4

## Git Tag
A git tag `v0.2.4` has been created locally and is ready to be pushed.

## Next Steps
To complete the release, the tag needs to be pushed to the remote repository:
```bash
git push origin v0.2.4
```

Once the tag is pushed, a GitHub release can be created from the tag using the GitHub UI or CLI.
