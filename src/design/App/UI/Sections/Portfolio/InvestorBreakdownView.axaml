<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:portfolio="clr-namespace:App.UI.Sections.Portfolio"
             xmlns:i="https://github.com/projektanker/icons.avalonia"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="600"
             x:Class="App.UI.Sections.Portfolio.InvestorBreakdownView"
             x:DataType="portfolio:InvestorBreakdownViewModel">

    <UserControl.Styles>
        <!-- Kill FluentTheme chrome on modal buttons (house pattern, see RecoveryModalsView) -->
        <Style Selector="Button.ModalBtn">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
        </Style>
        <Style Selector="Button.ModalBtn:pointerover /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style Selector="Button.ModalBtn:pressed /template/ ContentPresenter#PART_ContentPresenter">
            <Setter Property="Background" Value="Transparent" />
        </Style>
    </UserControl.Styles>

    <Border Classes="ModalCard Wide"
            MaxHeight="{Binding $parent[Panel].Bounds.Height, FallbackValue=800}"
            CornerRadius="16"
            Background="{DynamicResource RecoveryModalBg}"
            BorderBrush="{DynamicResource RecoveryModalBorder}"
            BorderThickness="1"
            BoxShadow="0 8 32 0 #40000000"
            ClipToBounds="True"
            Padding="0">
        <DockPanel>

            <!-- ── Sticky header: icon + title + close X ── -->
            <Border DockPanel.Dock="Top" Padding="24,20"
                    BorderThickness="0,0,0,1"
                    CornerRadius="15,15,0,0"
                    BorderBrush="{DynamicResource ModalHeaderBorder}"
                    Background="{DynamicResource RecoveryModalBg}">
                <Panel>
                    <StackPanel Spacing="4" VerticalAlignment="Center" Margin="0,0,40,0">
                        <Grid ColumnDefinitions="Auto,12,*">
                            <i:Icon Grid.Column="0"
                                   Value="fa-solid fa-chart-pie"
                                   FontSize="20"
                                   Foreground="{DynamicResource PrimaryGreenBrush}"
                                   VerticalAlignment="Center" />
                            <TextBlock Classes="Size-20" Grid.Column="2"
                                       Text="Investor Breakdown"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TextStrong}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center" />
                        </Grid>
                        <TextBlock Classes="Size-13" Text="{Binding ProjectName}"
                                   Foreground="{DynamicResource TextMuted}"
                                   TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding ProjectName}" />
                    </StackPanel>
                    <Button Name="CloseButtonX" Classes="ModalBtn ModalCloseBtn"
                            HorizontalAlignment="Right" VerticalAlignment="Top" />
                </Panel>
            </Border>

            <!-- ── Sticky footer: full-width Close (52px tall on mobile via MobileAction) ── -->
            <Border DockPanel.Dock="Bottom" Padding="24,16"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,15,15"
                    BorderBrush="{DynamicResource ModalHeaderBorder}"
                    Background="{DynamicResource RecoveryModalBg}">
                <Button Name="CloseButton" Classes="ModalBtn MobileAction" Cursor="Hand"
                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Border CornerRadius="8" Padding="24,12" HorizontalAlignment="Stretch"
                            Background="{DynamicResource ModalCancelBg}">
                        <TextBlock Classes="Size-S" Text="Close" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ModalCancelFg}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   TextAlignment="Center" />
                    </Border>
                </Button>
            </Border>

            <!-- ── Scrolling content ── -->
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <Panel Margin="24">

                    <!-- LOADING state: shown while shares are being fetched (optimistic open) -->
                    <StackPanel Spacing="12" IsVisible="{Binding IsLoading}"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                MinHeight="160" Margin="0,48">
                        <!-- Fixed-size container: the rotating icon's bounding box grows
                             to ~34px mid-rotation and must not escape its parent -->
                        <Panel Width="40" Height="40" HorizontalAlignment="Center">
                            <i:Icon Value="fa-solid fa-spinner" FontSize="24"
                                    Foreground="{DynamicResource PrimaryGreenBrush}"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    RenderTransformOrigin="50%,50%">
                                <i:Icon.RenderTransform><RotateTransform /></i:Icon.RenderTransform>
                                <i:Icon.Styles>
                                    <Style Selector="i|Icon[IsVisible=True]">
                                        <Style.Animations>
                                            <Animation Duration="0:0:1" IterationCount="INFINITE">
                                                <KeyFrame Cue="0%"><Setter Property="RotateTransform.Angle" Value="0" /></KeyFrame>
                                                <KeyFrame Cue="100%"><Setter Property="RotateTransform.Angle" Value="360" /></KeyFrame>
                                            </Animation>
                                        </Style.Animations>
                                    </Style>
                                </i:Icon.Styles>
                            </i:Icon>
                        </Panel>
                        <TextBlock Classes="Size-S" Text="Loading investor data..."
                                   Foreground="{DynamicResource TextMuted}"
                                   HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- ERROR state -->
                    <StackPanel Spacing="12" IsVisible="{Binding HasError}"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                MinHeight="160" Margin="0,48">
                        <i:Icon Value="fa-solid fa-triangle-exclamation" FontSize="24"
                                Foreground="{DynamicResource PillOrangeText}"
                                HorizontalAlignment="Center" />
                        <TextBlock Classes="Size-S" Text="Could not load investor data. Please try again later."
                                   Foreground="{DynamicResource TextMuted}"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- DATA state -->
                    <StackPanel Spacing="20" IsVisible="{Binding HasData}">

                        <!-- Summary stats -->
                        <Grid Name="SummaryStatsGrid" ColumnDefinitions="*,16,*" RowDefinitions="Auto,Auto">
                            <Border Name="StatCardInvested" Grid.Column="0" Grid.Row="0"
                                    Background="{DynamicResource SurfaceLow}"
                                    BorderBrush="{DynamicResource Stroke}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="16">
                                <StackPanel Spacing="4">
                                    <TextBlock Classes="Size-XS" Text="Total Invested"
                                               Foreground="{DynamicResource TextMuted}" />
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock Classes="Size-L" Text="{Binding TotalInvested}"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextStrong}" />
                                        <TextBlock Classes="Size-XXS" Text="{Binding CurrencySymbol}"
                                                   Foreground="{DynamicResource TextMuted}"
                                                   VerticalAlignment="Bottom" Margin="0,0,0,1" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <Border Name="StatCardInvestors" Grid.Column="2" Grid.Row="0"
                                    Background="{DynamicResource SurfaceLow}"
                                    BorderBrush="{DynamicResource Stroke}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="16">
                                <StackPanel Spacing="4">
                                    <TextBlock Classes="Size-XS" Text="Total Investors"
                                               Foreground="{DynamicResource TextMuted}" />
                                    <TextBlock Classes="Size-L" Text="{Binding TotalInvestors}"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextStrong}" />
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Context note for Fund projects -->
                        <Border Background="{DynamicResource PillOrangeBg}"
                                CornerRadius="8"
                                Padding="12"
                                IsVisible="{Binding IsFundType}">
                            <!-- Grid (not horizontal StackPanel) so the wrapping note
                                 text is width-constrained and cannot overflow -->
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="8">
                                <i:Icon Grid.Column="0" Value="fa-solid fa-circle-info"
                                       FontSize="14"
                                       Foreground="{DynamicResource PillOrangeText}"
                                       VerticalAlignment="Top" Margin="0,2,0,0" />
                                <TextBlock Classes="Size-13" Grid.Column="1" Text="{Binding ShareContextNote}"
                                           Foreground="{DynamicResource TextMuted}"
                                           TextWrapping="Wrap" />
                            </Grid>
                        </Border>

                        <!-- ── Desktop table (hidden on mobile) ── -->
                        <StackPanel Name="BreakdownTableDesktop" Spacing="12">
                            <!-- Table header -->
                            <Grid ColumnDefinitions="40,*,120,80,80"
                                  Margin="0,4,0,0">
                                <TextBlock Classes="Size-XS" Grid.Column="0" Text="#" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMuted}" />
                                <TextBlock Classes="Size-XS" Grid.Column="1" Text="Investor" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMuted}" />
                                <TextBlock Classes="Size-XS" Grid.Column="2" Text="Invested" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMuted}"
                                           HorizontalAlignment="Right" />
                                <TextBlock Classes="Size-XS" Grid.Column="3" Text="Share" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMuted}"
                                           HorizontalAlignment="Right" />
                                <TextBlock Classes="Size-XS" Grid.Column="4" Text="Claimed" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextMuted}"
                                           HorizontalAlignment="Right" />
                            </Grid>

                            <Border Height="1" Background="{DynamicResource Stroke}" />

                            <!-- Investor rows -->
                            <ItemsControl ItemsSource="{Binding Investors}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="portfolio:InvestorShareRowViewModel">
                                        <Panel Margin="0,2">
                                            <!-- Highlighted row for current user -->
                                            <Border Padding="8,8"
                                                    CornerRadius="8"
                                                    Background="{DynamicResource TabBarPillActiveBg}"
                                                    BorderBrush="{DynamicResource PrimaryGreenBrush}"
                                                    BorderThickness="1.5"
                                                    IsVisible="{Binding IsCurrentUser}">
                                                <Grid ColumnDefinitions="40,*,120,80,80">
                                                    <TextBlock Classes="Size-13" Grid.Column="0" Text="{Binding Rank}" FontWeight="Bold"
                                                               Foreground="{DynamicResource PrimaryGreenBrush}"
                                                               VerticalAlignment="Center" />
                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                                        <TextBlock Classes="Size-13" Text="{Binding ShortKey}" FontWeight="Bold"
                                                                   FontFamily="Consolas,Courier New,monospace"
                                                                   Foreground="{DynamicResource TextStrong}"
                                                                   TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding ShortKey}" />
                                                        <Border Background="{DynamicResource PrimaryGreenBrush}"
                                                                CornerRadius="4"
                                                                Padding="6,2">
                                                            <TextBlock Classes="Size-10" Text="You"
                                                                       FontWeight="Bold"
                                                                       Foreground="White" />
                                                        </Border>
                                                    </StackPanel>
                                                    <TextBlock Classes="Size-13" Grid.Column="2" Text="{Binding TotalInvested}" FontWeight="Bold"
                                                               Foreground="{DynamicResource TextStrong}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Classes="Size-13" Grid.Column="3" Text="{Binding SharePercentage}" FontWeight="Bold"
                                                               Foreground="{DynamicResource PrimaryGreenBrush}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Classes="Size-13" Grid.Column="4" Text="{Binding ClaimedPercentage}" FontWeight="Bold"
                                                               Foreground="{DynamicResource PillOrangeText}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                </Grid>
                                            </Border>
                                            <!-- Normal row for other investors -->
                                            <Border Padding="8,8"
                                                    BorderBrush="{DynamicResource Stroke}"
                                                    BorderThickness="0,0,0,1"
                                                    IsVisible="{Binding !IsCurrentUser}">
                                                <Grid ColumnDefinitions="40,*,120,80,80">
                                                    <TextBlock Classes="Size-13" Grid.Column="0" Text="{Binding Rank}"
                                                               Foreground="{DynamicResource TextMuted}"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Classes="Size-13" Grid.Column="1" Text="{Binding ShortKey}"
                                                               FontFamily="Consolas,Courier New,monospace"
                                                               Foreground="{DynamicResource TextStrong}"
                                                               VerticalAlignment="Center"
                                                               TextTrimming="CharacterEllipsis" ToolTip.Tip="{Binding ShortKey}" />
                                                    <TextBlock Classes="Size-13" Grid.Column="2" Text="{Binding TotalInvested}"
                                                               Foreground="{DynamicResource TextStrong}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Classes="Size-13" Grid.Column="3" Text="{Binding SharePercentage}" FontWeight="Bold"
                                                               Foreground="{DynamicResource PrimaryGreenBrush}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Classes="Size-13" Grid.Column="4" Text="{Binding ClaimedPercentage}"
                                                               Foreground="{DynamicResource PillOrangeText}"
                                                               HorizontalAlignment="Right"
                                                               VerticalAlignment="Center" />
                                                </Grid>
                                            </Border>
                                        </Panel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- ── Mobile cards (shown on compact; house pattern: table→cards) ── -->
                        <ItemsControl Name="BreakdownCardsMobile" ItemsSource="{Binding Investors}" IsVisible="False">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="portfolio:InvestorShareRowViewModel">
                                    <Border Margin="0,0,0,10"
                                            CornerRadius="10"
                                            Padding="14,12"
                                            Background="{DynamicResource SurfaceLow}"
                                            BorderBrush="{DynamicResource Stroke}"
                                            BorderThickness="1"
                                            Classes.CurrentUserCard="{Binding IsCurrentUser}">
                                        <Border.Styles>
                                            <Style Selector="Border.CurrentUserCard">
                                                <Setter Property="Background" Value="{DynamicResource TabBarPillActiveBg}" />
                                                <Setter Property="BorderBrush" Value="{DynamicResource PrimaryGreenBrush}" />
                                                <Setter Property="BorderThickness" Value="1.5" />
                                            </Style>
                                        </Border.Styles>
                                        <StackPanel Spacing="8">
                                            <!-- Row 1: rank + key + You badge -->
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <TextBlock Classes="Size-13" Text="{Binding Rank, StringFormat='#{0}'}" FontWeight="Bold"
                                                           Foreground="{DynamicResource TextMuted}"
                                                           VerticalAlignment="Center" />
                                                <TextBlock Classes="Size-13" Text="{Binding ShortKey}" FontWeight="SemiBold"
                                                           FontFamily="Consolas,Courier New,monospace"
                                                           Foreground="{DynamicResource TextStrong}"
                                                           TextTrimming="CharacterEllipsis"
                                                           VerticalAlignment="Center" ToolTip.Tip="{Binding ShortKey}" />
                                                <Border Background="{DynamicResource PrimaryGreenBrush}"
                                                        CornerRadius="4" Padding="6,2"
                                                        IsVisible="{Binding IsCurrentUser}"
                                                        VerticalAlignment="Center">
                                                    <TextBlock Classes="Size-10" Text="You" FontWeight="Bold" Foreground="White" />
                                                </Border>
                                            </StackPanel>
                                            <!-- Row 2: invested -->
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Classes="Size-XS" Grid.Column="0" Text="Invested"
                                                           Foreground="{DynamicResource TextMuted}" />
                                                <TextBlock Classes="Size-13" Grid.Column="1" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource TextStrong}">
                                                    <Run Text="{Binding TotalInvested}" /><Run Text=" " /><Run Text="{Binding CurrencySymbol}" />
                                                </TextBlock>
                                            </Grid>
                                            <!-- Row 3: share + claimed -->
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Classes="Size-XS" Grid.Column="0" Text="Share"
                                                           Foreground="{DynamicResource TextMuted}" />
                                                <TextBlock Classes="Size-13" Grid.Column="1" Text="{Binding SharePercentage}" FontWeight="Bold"
                                                           Foreground="{DynamicResource PrimaryGreenBrush}" />
                                            </Grid>
                                            <Grid ColumnDefinitions="*,Auto">
                                                <TextBlock Classes="Size-XS" Grid.Column="0" Text="Claimed"
                                                           Foreground="{DynamicResource TextMuted}" />
                                                <TextBlock Classes="Size-13" Grid.Column="1" Text="{Binding ClaimedPercentage}" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource PillOrangeText}" />
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                    </StackPanel>
                </Panel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
