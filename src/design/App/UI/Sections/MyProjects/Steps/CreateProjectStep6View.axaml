<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mp="clr-namespace:App.UI.Sections.MyProjects"
             xmlns:i="https://github.com/projektanker/icons.avalonia"
             mc:Ignorable="d" d:DesignWidth="680" d:DesignHeight="900"
             x:Class="App.UI.Sections.MyProjects.Steps.CreateProjectStep6View"
             x:DataType="mp:CreateProjectViewModel">

    <!-- ═══════════════════════════════════════ -->
    <!-- STEP 6: REVIEW & DEPLOY                  -->
    <!-- ═══════════════════════════════════════ -->
    <StackPanel Spacing="24">

        <!-- Review: Project Profile -->
        <Border CornerRadius="12" BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                Background="{DynamicResource BrandWash}" ClipToBounds="True">
            <StackPanel>
                <Border Padding="24" CornerRadius="11,11,0,0"
                        Background="{DynamicResource PrimaryGreenBrush08}">
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Border Width="36" Height="36" CornerRadius="8"
                                Background="{DynamicResource Accent}">
                            <i:Icon Value="fa-solid fa-circle-user"
                                   FontSize="18"
                                   Foreground="White" />
                        </Border>
                        <TextBlock Classes="Size-M" Text="Project Profile" FontWeight="Bold"
                                   Foreground="{DynamicResource TextStrong}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                <StackPanel Spacing="16" Margin="24">
                    <!-- Name -->
                    <Grid ColumnDefinitions="140,*">
                        <TextBlock Classes="Size-S" Text="Name"
                                   Foreground="{DynamicResource Accent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1" Text="{Binding ProjectName}"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right" />
                    </Grid>
                    <!-- About -->
                    <Grid ColumnDefinitions="140,*">
                        <TextBlock Classes="Size-S" Text="About"
                                   Foreground="{DynamicResource Accent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1" Text="{Binding ProjectAbout}"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right" TextWrapping="Wrap" />
                    </Grid>
                    <!-- Website -->
                    <Grid ColumnDefinitions="140,*">
                        <TextBlock Classes="Size-S" Text="Website"
                                   Foreground="{DynamicResource Accent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1" Text="{Binding ProjectWebsite}"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Review: Funding Configuration -->
        <Border CornerRadius="12" BorderBrush="{DynamicResource BitcoinAccent}" BorderThickness="1"
                Background="{DynamicResource BitcoinWash}" ClipToBounds="True">
            <StackPanel>
                <Border Padding="24" CornerRadius="11,11,0,0">
                    <Border.Background>
                        <SolidColorBrush Color="{StaticResource BitcoinOrange}" Opacity="0.08" />
                    </Border.Background>
                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Border Width="36" Height="36" CornerRadius="8"
                                Background="{DynamicResource BitcoinAccent}">
                            <i:Icon Value="fa-brands fa-bitcoin"
                                   FontSize="18"
                                   Foreground="White" />
                        </Border>
                        <TextBlock Classes="Size-M" Text="Funding Configuration" FontWeight="Bold"
                                   Foreground="{DynamicResource TextStrong}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>
                <StackPanel Spacing="16" Margin="24">
                    <!-- Target Amount -->
                    <Grid ColumnDefinitions="140,*">
                        <TextBlock Classes="Size-S" Text="{Binding TargetLabel}"
                                   Foreground="{DynamicResource BitcoinAccent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0} BTC">
                                    <Binding Path="TargetAmount" />
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </Grid>
                    <!-- Start Date -->
                    <Grid ColumnDefinitions="140,*">
                        <TextBlock Classes="Size-S" Text="Start Date"
                                   Foreground="{DynamicResource BitcoinAccent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1" Text="{Binding StartDate}"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right" />
                    </Grid>
                    <!-- End Date (investment only) -->
                    <Grid ColumnDefinitions="140,*" IsVisible="{Binding IsInvestment}">
                        <TextBlock Classes="Size-S" Text="End Date"
                                   Foreground="{DynamicResource BitcoinAccent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1" Text="{Binding EndDate}"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right" />
                    </Grid>
                    <!-- Penalty Days (investment and fund) -->
                    <Grid ColumnDefinitions="140,*" IsVisible="{Binding !IsSubscription}">
                        <TextBlock Classes="Size-S" Text="Penalty Days"
                                   Foreground="{DynamicResource BitcoinAccent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right">
                            <Run Text="{Binding PenaltyDays, Mode=OneWay}" /><Run Text=" days" />
                        </TextBlock>
                    </Grid>
                    <!-- Approval Threshold (fund/subscription only) -->
                    <Grid ColumnDefinitions="140,*" IsVisible="{Binding !IsInvestment}">
                        <TextBlock Classes="Size-S" Text="Approval Threshold"
                                   Foreground="{DynamicResource BitcoinAccent}" />
                        <TextBlock Classes="Size-S" Grid.Column="1"
                                   Foreground="{DynamicResource TextStrong}" FontWeight="Bold"
                                   HorizontalAlignment="Right">
                            <Run Text="{Binding ApprovalThreshold, Mode=OneWay}" /><Run Text=" BTC" />
                        </TextBlock>
                    </Grid>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Review: Funding Stages -->
        <Border CornerRadius="12" BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                Background="{DynamicResource BrandWash}" ClipToBounds="True">
            <StackPanel>
                <Border Padding="24" CornerRadius="11,11,0,0"
                        Background="{DynamicResource PrimaryGreenBrush08}">
                    <DockPanel>
                        <StackPanel Orientation="Horizontal" Spacing="12" DockPanel.Dock="Left">
                            <Border Width="36" Height="36" CornerRadius="8"
                                    Background="{DynamicResource Accent}">
                                <i:Icon Value="fa-regular fa-calendar" FontSize="18"
                                        Foreground="White"
                                        HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <TextBlock Classes="Size-M" Text="{Binding ScheduleTitle}" FontWeight="Bold"
                                       Foreground="{DynamicResource TextStrong}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        <!-- Payout summary text (e.g. "Monthly payouts (3 installments)") -->
                        <TextBlock Classes="Size-13" Text="{Binding ScheduleSummary}"
                                   Foreground="{DynamicResource Accent}"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"
                                   IsVisible="{Binding !IsInvestment}" />
                    </DockPanel>
                </Border>
                <StackPanel Spacing="4" Margin="24">
                    <ItemsControl ItemsSource="{Binding Stages}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="mp:ProjectStageViewModel">
                                <Border Padding="14,10" Margin="0,0,0,4"
                                        CornerRadius="10"
                                        BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                                        Background="{DynamicResource PrimaryGreenBrush04}">
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                        <TextBlock Classes="Size-S" Grid.Column="0" Foreground="{DynamicResource Accent}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"
                                                   TextWrapping="NoWrap">
                                            <Run Text="{Binding StageLabel, Mode=OneWay}" /><Run Text=" " /><Run Text="{Binding StageNumber, Mode=OneWay}" />
                                        </TextBlock>
                                        <TextBlock Classes="Size-S" Grid.Column="1" Foreground="{DynamicResource Accent}"
                                                   FontWeight="Bold"
                                                   HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"
                                                   TextAlignment="Right"
                                                   TextWrapping="Wrap">
                                            <Run Text="{Binding Percentage, Mode=OneWay}" /><Run Text=" paid on " /><Run Text="{Binding ReleaseDate, Mode=OneWay}" />
                                        </TextBlock>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- ═══════════════════════════════════════════════ -->
        <!-- DEPLOY — Vue: review-card-deploy with Angor logo -->
        <!-- Deploy Now button is in the FOOTER, not here      -->
        <!-- ═══════════════════════════════════════════════ -->
        <Border CornerRadius="12" BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                Background="{DynamicResource BrandWash}" ClipToBounds="True"
                Padding="30">
            <StackPanel Spacing="16" HorizontalAlignment="Center">
                <!-- Angor logo circle — matches Vue deploy card -->
                <Border Width="64" Height="64" CornerRadius="32"
                        HorizontalAlignment="Center"
                        Background="{DynamicResource PrimaryGreenBrush12}">
                    <Viewbox Width="32" Height="32">
                        <Svg Path="/Assets/logo.svg" />
                    </Viewbox>
                </Border>

                <TextBlock Classes="Size-20" Text="Ready to Deploy" FontWeight="Bold"
                           Foreground="{DynamicResource TextStrong}" TextAlignment="Center"
                           HorizontalAlignment="Center" />
                <TextBlock Classes="Size-S" Text="Your project is ready to be deployed on-chain. Click Deploy Now in the footer to begin." Foreground="{DynamicResource TextMuted}"
                           TextAlignment="Center" HorizontalAlignment="Center"
                           TextWrapping="Wrap" MaxWidth="400" />
            </StackPanel>
        </Border>
    </StackPanel>

</UserControl>
