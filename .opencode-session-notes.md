# Session Notes — 2026-07-14 (branch: ui-bug-fixes, PR #927)

Untracked scratch file for agent context. Do not commit.

## State: all work committed & pushed, PR #927 updated

PR: https://github.com/block-core/angor/pull/927 — title now covers all scope.
Branch `ui-bug-fixes`, 4 new commits on top of the original 3 UI fixes:

- `0928be82` feat: live funders monitoring with badges, notifications and loading states
- `fcd911f4` feat: markdown rendering for project descriptions
- `932f01ce` perf: decode remote images at bounded width to fix scroll lag
- `23fa16bf` fix: stale project banners/avatars via stale-while-revalidate metadata cache

Desktop app was run locally for user testing:
`dotnet run --project src/design/App.Desktop/App.Desktop.csproj --no-build -c Debug`
(log: /tmp/angor-desktop.log; kill via `pkill -f App.Desktop`). User confirmed "works".

## What was built (key files)

### 1. Funders live monitoring
- `src/design/App/UI/Shared/Services/FundersMonitor.cs` — NEW singleton (DI in
  CompositionRoot.cs). Polls all founder projects every 2 min + on WalletsUpdated
  (throttled 3s), warm snapshot, diff detection (new pending request / new direct
  investment / approved→Invested transition), events `Updated` + `NotificationRaised`
  marshalled via Dispatcher.UIThread.Post. `MarkApprovedLocally(eventId)` overlay
  prevents approved cards flipping back to waiting before the Nostr approval DM syncs.
- `FundersViewModel` rewritten: renders from monitor snapshot instantly; RefreshAsync
  awaits monitor sync so spinner reflects real duration. Session-local reject overlay
  (`_locallyRejected`), expanded-state preserved by EventId. `HasEverLoaded` gate:
  full-page loader ONLY before first poll ever; afterwards empty/data + spinning icon
  (relay syncs take 30s+; earlier version showed perpetual loader when 0 funders).
- `ApprovedLabel` on cards: "Funded" vs "Awaiting funding" (IsFunded = indexer-confirmed).
- Badges: `NavItem` converted record→reactive class with BadgeCount/HasBadge/BadgeText
  (ShellViewModel.cs ~L365); desktop sidebar badge in ShellView.axaml nav DataTemplate;
  mobile FounderBadge bound to ShellViewModel.ShowFounderBadge/FounderBadgeText.
  ShellViewModel injects FundersMonitor, starts it in ctor, toasts on notifications.
- CRITICAL bug found: desktop uses ContentControl path (`CurrentSectionContent` →
  `GetOrCreateView`), NOT SectionPanel (mobile-only) — `OnBecameActive` was never
  called on desktop. Fixed for Funders via onReuse callback (ShellViewModel.cs ~L1429).
  NOTE: other sections may have the same latent issue (Home/FindProjects have their own
  onReuse handlers; Funds does not).

### 2. Markdown rendering
- `src/design/App/UI/Shared/Controls/MarkdownTextBlock.cs` — NEW, dependency-free
  (user chose custom over Markdown.Avalonia). StackPanel-based. Supports: #headings,
  **bold**, *italic*, `code`, fenced blocks, links (InlineUIContainer + ExplorerHelper
  .OpenUrl, http(s) only), ul/ol lists WITH lazy continuation (hard-wrapped lines join
  item), blockquotes, ---rules. Media: `![alt](url)`, HTML `<img src w h>` (consecutive
  images → WrapPanel row), `<video>` → "▶ Watch video" external-open card, `<a>` → link,
  `<br>` → newline. Theme brushes resolved via TryFindResource (TextMutedBrush,
  TextStrongBrush, Brand, SurfaceMedium). `MarkdownMedia` helper class same file.
- `src/design/App/UI/Shared/Helpers/MarkdownToPlainConverter.cs` — NEW, strips md+HTML
  for clamped summaries. Used in: ProjectCard.axaml (2-line), ProjectDetailView hero
  (1-line), InvestmentDetailView (3-line).
- Wired: ProjectDetailView.axaml long description uses MarkdownTextBlock.

### 3. Image decode perf (scroll lag / "Reputation" oversized logo)
- ImageCacheService: `LoadBitmapAsync(url, cb, decodeWidth)`; memory+inflight cache
  keys include width (`CacheKey`). Callers: ProjectCard 640/128, FindProjects VM &
  Portfolio VM 1280/256, EditProfile 1280/256, CreateProjectStep3 1280/256.
- Find Projects grid still NOT virtualized (plain ItemsControl + Load More) — next
  lever if lag persists.

### 4. Stale profile metadata (banners/avatars never updating)
- Root cause: LiteDB Project cache write-once; GetAllAsync short-circuited on full
  cache hit, kind-0 never refetched.
- `Project.MetadataFetchedAt` (new field, defaults MinValue=stale for existing docs).
- `DocumentProjectService`: `MetadataTtl` 1h; `RevalidateStaleMetadata()` fire-and-
  forget background kind-0 refetch (npub only) + upsert; `RevalidationsInFlight` dedup.
  New `IProjectService.InvalidateCacheAsync(id)` (deletes doc).
- `UpdateProjectProfile` invalidates cache after publish (founder sees own edit).
- `FindProjectsViewModel.ApplyProfileData` now applies fresh Metadata.Banner/Picture
  (previously dropped) — detail open updates visuals immediately.

## Conventions learned
- App project GlobalUsings has ReactiveUI but `RxApp` didn't resolve in
  UI/Shared/Services — app convention is `Avalonia.Threading.Dispatcher.UIThread.Post`.
- DockPanel: last child fills; docked children must come first in markup.
- Theme brush keys end in "Brush" (TextMutedBrush); "Brand" is a Color resource.
- Build check: `dotnet build src/design/App/App.csproj` (App.sln fails locally —
  iOS workload not installed, unrelated).
- Tests: `dotnet test src/sdk/Angor.Sdk.Tests/...` → 323 pass.

## Open items / possible follow-ups
- .DS_Store files untracked and littering repo — suggest .gitignore entry.
- Find Projects list virtualization (if scroll perf still poor).
- Inline video playback would need LibVLCSharp.Avalonia (user declined for now).
- `OnBecameActive` desktop gap may affect other sections (e.g. "Funds").
- FundersMonitor poll duration can be long (30s Nostr timeout per project);
  could parallelize per-project syncs or expose per-project progress.
- Rejects are session-local only (no protocol-level reject exists).
- UAT tests not run this session (only builds + SDK unit tests + manual testing).
